export const showPanel = (state) => state.Comments.showPanel;
export const comments = (state) => state.Comments.comments;
export const taskList = (state) => state.Comments.taskList;
export const projectList = (state) => state.Comments.projectList;
export const selectedTaskId = (state) => state.Comments.selectedTaskId;
export const selectedProjectId = (state) => state.Comments.selectedProjectId;
